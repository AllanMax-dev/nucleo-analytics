# Dicionário de dados

Este documento vai registrar as tabelas, campos, tipos de dados e descrições utilizadas no projeto.

Como os dados ainda não foram gerados nesta etapa, o dicionário será preenchido depois da definição do modelo de dados.
